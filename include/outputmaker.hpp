#pragma once

#include <vector>
#include <algorithm>
#include <iostream>
#include <fstream>
#include <string>

class outputmaker
{
private:
    /* data */
public:
    outputmaker(/* args */);
    ~outputmaker();
};

outputmaker::outputmaker(/* args */)
{
}

outputmaker::~outputmaker()
{
}
